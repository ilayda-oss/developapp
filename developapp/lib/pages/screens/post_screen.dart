import 'package:flutter/material.dart';

class PostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color:
          Color(0xFFFDE456), // Replace this with your actual home page content
      child: Center(
        child: Text('Posting Page'),
      ),
    );
  }
}
